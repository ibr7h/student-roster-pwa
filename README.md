# سجلات الطلاب PWA

تطبيق عربي محلي لإدارة سجلات الطلاب والتقييمات والحضور وجدول المعلم، مع واجهة محسنة لـ iPhone وSafari ودعم العمل كتطبيق PWA.

## الإصدار الحالي

**v4.16.2 — Automatic Behavior Recurrence**

ينظم هذا الإصدار التقييمات في أرشيف شهري قابل للطي مع بحث وفلترة حسب النوع والفترة وحالة اكتمال الرصد، دون تغيير بيانات الدرجات أو نموذج التخزين.

## الوظائف الرئيسية

- تقييمات زمنية ودرجات مستقلة لكل نشاط.
- حضور يومي وتقارير شهرية وفصلية.
- إدارة الفصول والطلاب واستيراد CSV.
- جداول متعددة للمعلم مع فترات سريان ومناوبة وإشراف.
- تقارير A4 وحفظ PDF.
- حفظ محلي في IndexedDB مع fallback إلى localStorage.
- نسخ احتياطي واستعادة JSON.
- PWA يعمل دون اتصال بعد التحميل الأول.

## بنية المشروع

- `index.html` — هيكل الواجهة والنوافذ.
- `app.js` — حالة التطبيق ومنطق التقييمات والحضور والجداول والتقارير.
- `styles.css` — أنماط الشاشة والاستجابة للجوال.
- `print.css` — تنسيقات الطباعة وA4.
- `sw.js` — التخزين المؤقت والتحديث التلقائي.
- `version.js` — رقم الإصدار المنشور.
- `scripts/validate.cjs` — فحص جودة بدون مكتبات خارجية.

## فحص الجودة

```bash
npm run validate
```

يتحقق الفحص من سلامة JavaScript، وتطابق رقم الإصدار، وعدم تكرار معرفات HTML، ووجود ملفات PWA المحلية، ومنع أخطاء selectors المتكررة.

## الخصوصية

لا توجد قاعدة بيانات خارجية في هذه النسخة. سجلات الطلاب محفوظة على جهاز المستخدم. يوصى بإنشاء نسخة احتياطية JSON دوريًا لأن حذف بيانات المتصفح قد يحذف التخزين المحلي.

## الحقوق

Copyright © 2026 Ibrahim Alneami — All Rights Reserved.


### السلوك والانضباط — v4.16.0

- رصد المخالفات السلوكية حسب المرحلة والدرجة وفق الإصدار الخامس 1447هـ / 2025م.
- حفظ التاريخ والحصة والإجراء ومدى الاستجابة والتكرار وجهة الإحالة.
- إحالة داخلية اختيارية لإدارة المدرسة، بما فيها وكيل الشؤون التعليمية.
- نموذج رصد المعلم لمشكلة سلوكية، ونموذج إحالة داخلي، ومسودة النموذج السري الوزاري للإحالة.
- لا يخصم التطبيق درجات السلوك آليًا؛ يعرض الحسم المرجعي ويترك اعتماد الإجراء للجهة المخولة.
- السجلات السلوكية محفوظة محليًا وتدخل ضمن النسخ الاحتياطية.


### رصد المخالفة من شاشة الحضور — v4.16.1

- زر «⚠ مخالفة» أمام كل طالب في شاشة الحضور.
- يحدد الطالب وتاريخ شاشة الحضور تلقائيًا.
- يحدد الحصة الحالية تلقائيًا فقط عندما يطابق الجدول الفصل الحالي، أو يختار الحصة الوحيدة لذلك الفصل في اليوم.
- إذا تعددت الحصص ولم يمكن الجزم، تبقى الحصة اختيارية بدل التخمين.
- يمكن تعديل التاريخ والحصة يدويًا قبل حفظ الرصد.


### التكرار التلقائي للمخالفات — v4.16.2

- إزالة إدخال عدد مرات التكرار يدويًا.
- حساب ترتيب تكرار نفس المخالفة تلقائيًا للطالب حسب التاريخ والحصة.
- عرض «المرة الأولى/الثانية/…» وعدد السوابق لنفس المخالفة.
- عرض إجمالي مخالفات الطالب بشكل مستقل.
- قسم «عرض السوابق» يبين تاريخ وحصة كل واقعة سابقة.
- التقارير والنماذج تستخدم رقم التكرار المحسوب من السجل الفعلي، لا القيمة اليدوية.
